import 'package:flutter/material.dart';

class VerifLogPage extends StatelessWidget {
  const VerifLogPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Verification Log Page"),
    );
  }
}
